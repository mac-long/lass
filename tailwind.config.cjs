const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {
			colors: {
				primary: '#A0FF1F'
			},
			fontFamily: {
				sans: ['Poppins']
			}
		}
	},
	plugins: []
};

module.exports = config;
